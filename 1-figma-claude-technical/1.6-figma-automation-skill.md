---
name: figma-automation
description: "Figma Automation & Text-Operationen mit Claude. NUTZE DIESEN SKILL bei JEDER Figma-Erwähnung: Texte übersetzen, korrigieren, Batch-Operationen, Edit-Links verarbeiten, Screenshots validieren, Nodes bearbeiten, Variables binden, Design-Prinzipien anwenden. Beim Laden automatisch die Referenzdateien von GitHub lesen und den Versionsstempel gegen die Repo-Version prüfen."
---

# figma-automation — Claude Skill

> **Installation:** Datei herunterladen → Claude.ai / Cowork → Einstellungen → Capabilities/Skills → Skill hinzufügen.
> Diese Repo-Version ist die **kanonische Quelle** — die installierte Kopie ist nur ein Spiegel.
> Format-Hinweis: Der YAML-Frontmatter-Block muss das Allererste in der Datei sein (kein Text davor, kein Code-Zaun darum) — sonst kann der Skill-Loader ihn nicht parsen.

## Version & sync check

**Version: 2026-07-21 17:32 CEST**

- At the start of every task in this project, fetch the canonical skill from
  `https://raw.githubusercontent.com/amyria3/claude-figma-automation/main/1-figma-claude-technical/1.6-figma-automation-skill.md`
  and compare its version stamp with the installed copy.
- If the stamps differ: follow the **GitHub version** for the task (it is the source of truth) and tell the user to update the installed skill from the repo file.
- Whenever you edit this file in the repo, update the version stamp above (date + time, Europe/Berlin) in the same commit.

## Setup

- Figma access: **Figma connector** in Cowork/claude.ai (Settings → Connectors → Figma). Write access runs via `use_figma` (remote MCP) — details: `1-figma-claude-technical/1.1-figma-schreibzugriff-use_figma.md`. Load the `figma-use` skill/resource before every `use_figma` call.
- Repo access: no installation needed — read reference files via raw URLs (`https://raw.githubusercontent.com/amyria3/claude-figma-automation/main/<path>`). GitHub MCP via Docker is legacy only: `4-setup-legacy/4.1-github-mcp-setup.md`.
- Writing to the repo: GitHub web editor via Claude in Chrome — techniques in `3-claude-meta/3.1-github-web-editor-notes.md`.

## Required reading at task start (via raw URLs)

| Order | File | Content |
|---|---|---|
| 1 | `1-figma-claude-technical/1.2-figma-workflow-technisch.md` | Pre-flight checklist, golden rules |
| 2 | `1-figma-claude-technical/1.3-figma-api-reference.md` | API reference, code patterns, limitations |
| 3 | `figma-design-principles-arkadieff.md` (repo root) | Design principles (flex, spacing, typography) |
| 4 | `1-figma-claude-technical/1.5-figma-stylesheet-workaround.md` | LEGACY — variable edge cases only |
| 5 | `1-figma-claude-technical/1.4-figma-kollaboration-workflows.md` | Recommended workflows (for workflow questions) |

Also: check `2-tarabao/offene-punkte.md` (Tarabao) and `1-figma-claude-technical/offene-punkte.md` (Claude/tech) at session start and update them before session end. When writing or correcting German text for the user, load `3-claude-meta/3.3-german-stilguide-claude.md`. Language rule: purely Claude-facing content in English (see README, Instructions for Claude).

> If raw URLs are unreachable: ask the user to paste the files as text. (Legacy alternative: GitHub MCP, see `4-setup-legacy/4.1-github-mcp-setup.md`.)

## Design principles (quick reference)

Full principles: `figma-design-principles-arkadieff.md`; token table: `2-tarabao/2.1-ux-ui-dokumentation-tarabao.md` (section "Design Tokens").

- **All layouts** as `flex-row` / `flex-col` (auto-layout), naming per Tailwind conventions (lowercase)
- **Spacing tokens (px):** 2 / 4 / 6 / 8 / 12 / 16 / 20 / 28 / 40 / 48 / 56 — collections `Lyt scl / Spcngs / Gaps` (`gap-*`) and `Lyt scl / Spcngs / Box Spacing` (`box-spacing-*`)
- **Values between token steps:** fall back to the Tailwind utility scale `Lyt scl / Tailwind-utility-units` (`[twuc]-N`, 1 twuc = 4px). Never leave gaps/paddings hardcoded in masters.
- **Variables:** work via the plugin API (fixed 2026-07-13) — always use the full ID prefix `VariableID:...`; see `1.3-figma-api-reference.md`

## Operations

- **Text:** translate, correct, replace, batch across nodes (font-load recipe first!)
- **Styling:** read & set colors, fills, strokes; bind variables to properties
- **Layout:** flex VERTICAL/HORIZONTAL, alignment, sizing
- **Nodes:** create, delete, rename, nest
- **Validation:** visual check via `get_screenshot` or `await node.screenshot()` (inline); `get_metadata` for structure (hierarchy, counts, positions — returns XML, no images)

## Known constraints

| Feature | Status |
|---|---|
| Variables read/write/create/bind | ✅ works via plugin API (full `VariableID:` prefix required) |
| Stylesheet workaround | LEGACY — only for text layers without existing variable binding or library text-style transfer, see `1.5-figma-stylesheet-workaround.md` |
| `figma.notify()`, `console.log()` as output | ❌ use `return` |
| Sync APIs (`getNodeById`, `getLocalVariableCollections`) | ❌ use async variants |
